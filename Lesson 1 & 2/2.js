const arr1 = ['me','you','her'] //if we add export on this we get it as an object 
const arr2 = [1,2,3] //if we add export on this we get it as an object 

module.exports = {
    arr1,arr2
} // with this we will get the array as an array in where its being imported